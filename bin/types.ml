type creet = { 
  mutable x: float; 
  mutable y: float;
  mutable vx: float;
  mutable vy: float;
  mutable radius: float;
  mutable color: string;
  mutable is_dead: bool;
  mutable is_infected: bool;
  mutable has_bounced: bool;
  mutable berserk: bool;
  mutable mean: bool;
  mutable is_dragging: bool;
  mutable infection_time: float;
}
